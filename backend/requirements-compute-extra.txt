# Library populer TAMBAHAN untuk image ch-compute (layer terpisah dari
# requirements-compute.txt agar cache layer lama tidak invalid).
# Kebijakan: semua library yang umum dipakai mahasiswa terpasang di image;
# hanya library yang sangat jarang dipakai yang di-install manual oleh user.
# CATATAN: mediapipe SENGAJA tidak dimasukkan (menarik opencv-python +
# opencv-contrib-python non-headless yang bentrok dengan headless di image).
# ultralytics menarik opencv-python non-headless -> ditimpa ulang dengan
# opencv-python-headless di akhir layer Dockerfile (force-reinstall --no-deps).

# --- NLP / teks (termasuk Bahasa Indonesia) ---
Sastrawi==1.0.1
wordcloud==1.9.6
emoji==2.15.0
deep-translator==1.11.4
thefuzz==0.22.1

# --- Computer vision / deep learning ---
ultralytics==8.4.104
timm==1.0.28
pytorch-lightning==2.6.5
torchmetrics==1.9.0
torchinfo==1.8.0
einops==0.8.2
tensorboard==2.21.0

# --- ML klasik / tuning / interpretasi ---
mlxtend==0.23.4
optuna==4.9.0
shap==0.49.1
lime==0.2.0.1
category_encoders==2.8.1
missingno==0.5.2

# --- Time series / finance ---
prophet==1.3.0
yfinance==1.5.1

# --- Geospasial ---
folium==0.20.0
geopandas==1.1.4

# --- Dokumen / file / utilitas ---
pypdf==6.14.2
python-docx==1.2.0
xlsxwriter
tabulate
kaggle==1.7.4.5
